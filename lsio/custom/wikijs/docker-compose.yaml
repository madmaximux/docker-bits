
# [Wikijs](https://github.com/Requarks/wiki) A modern, lightweight and powerful
# wiki app built on NodeJS.

---

services:
  wikijs:
    image: ghcr.io/linuxserver/wikijs
    container_name: wikijs
    environment:
      - PUID=${PUID:-1024}
      - PGID=${PGID:-100}
      - UMASK=${UMASK:-002}
      - TZ=${TZ:-America/Chicago}
      # - DB_TYPE=sqlite # optional
      # - DB_HOST= # optional
      # - DB_PORT= # optional
      # - DB_NAME= # optional
      # - DB_USER= # optional
      # - DB_PASS= # optional
    volumes:
      - ${DOCKERCONFIGDIR:-/volume1/docker/appdata}/wikijs:/config
      - ${DOCKERCONFIGDIR:-/volume1/docker/appdata}/wikijs/data:/data
    # ports:
      # - 3000:3000 # Port for Wiki.js's web interface.
    networks:
      default:
        aliases:
          - wikijs
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped

networks:
  default:
    name: ${NETWORK_NAME:-synobridge}
    external: true